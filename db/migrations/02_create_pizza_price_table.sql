CREATE TABLE pizza_price(
                      id SERIAL PRIMARY KEY,
                      small INTEGER,
                      medium INTEGER,
                      large INTEGER,
                      extra_large INTEGER
);